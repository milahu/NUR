error:
… while evaluating the attribute 'prismlauncher'

  at ./packages.nix:27:5:

    26|   minipkgs = {
    27|     prismlauncher =
      |     ^
    28|       if (builtins.compareVersions pkgs.prismlauncher.version minipkgs0.prismlauncher.version) >= 0 then

… while evaluating a branch condition

  at ./packages.nix:28:7:

    27|     prismlauncher =
    28|       if (builtins.compareVersions pkgs.prismlauncher.version minipkgs0.prismlauncher.version) >= 0 then
      |       ^
    29|         pkgs.prismlauncher

… in the argument of the not operator

… while calling the 'lessThan' builtin

  at ./packages.nix:28:96:

    27|     prismlauncher =
    28|       if (builtins.compareVersions pkgs.prismlauncher.version minipkgs0.prismlauncher.version) >= 0 then
      |                                                                                                ^
    29|         pkgs.prismlauncher

… while calling the 'compareVersions' builtin

  at ./packages.nix:28:11:

    27|     prismlauncher =
    28|       if (builtins.compareVersions pkgs.prismlauncher.version minipkgs0.prismlauncher.version) >= 0 then
      |           ^
    29|         pkgs.prismlauncher

… while evaluating the second argument passed to builtins.compareVersions

… while evaluating the attribute 'prismlauncher.version'

  at ./packages.nix:20:5:

    19|   minipkgs0 = rec {
    20|     prismlauncher = pkgs.callPackage ./pkgs/prismlauncher/package.nix {
      |     ^
    21|       prismlauncher-unwrapped = prismlauncher-unwrapped;

… from call site

  at ./packages.nix:20:21:

    19|   minipkgs0 = rec {
    20|     prismlauncher = pkgs.callPackage ./pkgs/prismlauncher/package.nix {
      |                     ^
    21|       prismlauncher-unwrapped = prismlauncher-unwrapped;

… while calling anonymous lambda

  at <nixpkgs> + /lib/customisation.nix:308:19:

   307|     in
   308|     autoArgs: fn: args:
      |                   ^
   309|     let

… from call site

  at <nixpkgs> + /lib/trivial.nix:1081:5:

  1080|     # TODO: Should we add call-time "type" checking like built in?
  1081|     __functor = self: f;
      |     ^
  1082|     __functionArgs = args;

… while calling 'f''

  at <nixpkgs> + /lib/customisation.nix:159:9:

   158|       f' =
   159|         origArgs:
      |         ^
   160|         let

… from call site

  at <nixpkgs> + /lib/customisation.nix:161:20:

   160|         let
   161|           result = f origArgs;
      |                    ^
   162|

… while calling anonymous lambda

  at ./pkgs/prismlauncher/package.nix:1:1:

     1| {
      | ^
     2|   addDriverRunpath,

… from call site

  at <nixpkgs> + /lib/trivial.nix:1081:5:

  1080|     # TODO: Should we add call-time "type" checking like built in?
  1081|     __functor = self: f;
      |     ^
  1082|     __functionArgs = args;

… while calling anonymous lambda

  at <nixpkgs> + /lib/customisation.nix:864:9:

   863|       (
   864|         fpargs:
      |         ^
   865|         transformDrv (

… from call site

  at <nixpkgs> + /lib/customisation.nix:865:9:

   864|         fpargs:
   865|         transformDrv (
      |         ^
   866|           constructDrv (extendsWithExclusion excludeDrvArgNames extendDrvArgs (toFunction fpargs))

… while calling 'id'

  at <nixpkgs> + /lib/trivial.nix:61:8:

    60|   */
    61|   id = x: x;
      |        ^
    62|

… from call site

  at <nixpkgs> + /lib/customisation.nix:866:11:

   865|         transformDrv (
   866|           constructDrv (extendsWithExclusion excludeDrvArgNames extendDrvArgs (toFunction fpargs))
      |           ^
   867|         )

… while calling 'mkDerivation'

  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:72:18:

    71|   */
    72|   mkDerivation = fnOrAttrs: makeDerivationExtensible (toFunction fnOrAttrs);
      |                  ^
    73|

… from call site

  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:72:29:

    71|   */
    72|   mkDerivation = fnOrAttrs: makeDerivationExtensible (toFunction fnOrAttrs);
      |                             ^
    73|

… while calling 'makeDerivationExtensible'

  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:83:5:

    82|   makeDerivationExtensible =
    83|     rattrs:
      |     ^
    84|     let

… from call site

  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:140:22:

   139|
   140|       finalPackage = mkDerivationSimple overrideAttrs args;
      |                      ^
   141|

… while calling 'mkDerivationSimple'

  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:771:5:

   770|     # * [package attributes]: https://nixos.org/manual/nix/stable/glossary#package-attribute-set
   771|     {
      |     ^
   772|

… from call site

  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:879:5:

   878|
   879|     extendDerivation validity.handled (
      |     ^
   880|       {

… while calling 'extendDerivation'

  at <nixpkgs> + /lib/customisation.nix:401:26:

   400|   extendDerivation =
   401|     condition: passthru: drv:
      |                          ^
   402|     let

… from call site

  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:849:16:

   848|           "`env` must be an attribute set of environment variables. Set `env.env` or pick a more specific name.";
   849|         assert assertMsg (overlappingNames == [ ])
      |                ^
   850|           "The `env` attribute set cannot contain any attributes passed to derivation. The following attributes are overlapping:\n${errors}";

… while calling 'assertMsg'

  at <nixpkgs> + /lib/asserts.nix:50:21:

    49|   # TODO(Profpatsch): add tests that check stderr
    50|   assertMsg = pred: msg: pred || throw msg;
      |                     ^
    51|

… from call site

  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:827:14:

   826|
   827|       meta = checkMeta.commonMeta {
      |              ^
   828|         inherit validity attrs pos;

… while calling 'commonMeta'

  at <nixpkgs> + /pkgs/stdenv/generic/check-meta.nix:520:5:

   519|   commonMeta =
   520|     {
      |     ^
   521|       validity,

… from call site

  at ./packages.nix:23:31:

    22|     };
    23|     prismlauncher-unwrapped = pkgs.callPackage ./pkgs/prismlauncher-unwrapped/package.nix {
      |                               ^
    24|     };

… while calling anonymous lambda

  at <nixpkgs> + /lib/customisation.nix:308:19:

   307|     in
   308|     autoArgs: fn: args:
      |                   ^
   309|     let

error: evaluation aborted with the following error message: 'lib.customisation.callPackageWith: Function called without required argument "extra-cmake-modules" at ./pkgs/prismlauncher-unwrapped/package.nix:7'
