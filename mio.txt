error:
… while evaluating the attribute 'brave-origin'
  at ./packages.nix:30:19:
    29|                   inherit name;
    30|                   value = self.callPackage (subdir + "/${name}/package.nix") { };
      |                   ^
    31|                 })

… from call site
  at ./packages.nix:30:27:
    29|                   inherit name;
    30|                   value = self.callPackage (subdir + "/${name}/package.nix") { };
      |                           ^
    31|                 })

… while calling anonymous lambda
  at <nixpkgs> + /lib/customisation.nix:307:19:
   306|     in
   307|     autoArgs: fn: args:
      |                   ^
   308|     let

… while calling a functor (an attribute set with a '__functor' attribute)
  at <nixpkgs> + /lib/customisation.nix:322:7:
   321|     if unpassedArgs == { } || all (value: value) (attrValues unpassedArgs) then
   322|       makeOverridable f allArgs
      |       ^
   323|     else

… from call site
  at <nixpkgs> + /lib/trivial.nix:1183:7:
  1182|     g: {
  1183|       __functor = self: g;
      |       ^
  1184|       __functionArgs = fArgs;

… while calling 'f''
  at <nixpkgs> + /lib/customisation.nix:158:9:
   157|       f' =
   158|         origArgs:
      |         ^
   159|         let

… while evaluating a branch condition
  at <nixpkgs> + /lib/customisation.nix:174:9:
   173|         in
   174|         if isAttrs result then
      |         ^
   175|           result

… while calling the 'isAttrs' builtin
  at <nixpkgs> + /lib/customisation.nix:174:12:
   173|         in
   174|         if isAttrs result then
      |            ^
   175|           result

… from call site
  at <nixpkgs> + /lib/customisation.nix:160:20:
   159|         let
   160|           result = f origArgs;
      |                    ^
   161|

… while calling anonymous lambda
  at ./by-name/br/brave-origin/package.nix:1:1:
     1| { callPackage }:
      | ^
     2| callPackage ./default.nix { }

… from call site
  at ./by-name/br/brave-origin/package.nix:2:1:
     1| { callPackage }:
     2| callPackage ./default.nix { }
      | ^
     3|

(7 duplicate frames omitted)

… while calling anonymous lambda
  at ./by-name/br/brave-origin/default.nix:1:1:
     1| { callPackage }:
      | ^
     2| callPackage ./browser.nix { }

… from call site
  at ./by-name/br/brave-origin/default.nix:2:1:
     1| { callPackage }:
     2| callPackage ./browser.nix { }
      | ^
     3|

(7 duplicate frames omitted)

… while calling anonymous lambda
  at ./by-name/br/brave-origin/browser.nix:1:1:
     1| {
      | ^
     2|   lib,

… while calling a functor (an attribute set with a '__functor' attribute)
  at ./by-name/br/brave-origin/browser.nix:36:17:
    35|
    36|   chromiumDrv = pkgs.chromium.override {
      |                 ^
    37|     upstream-info = chromiumInfo;

… from call site
  at <nixpkgs> + /lib/trivial.nix:1183:7:
  1182|     g: {
  1183|       __functor = self: g;
      |       ^
  1184|       __functionArgs = fArgs;

… while calling anonymous lambda
  at <nixpkgs> + /lib/customisation.nix:171:13:
   170|             */
   171|             newArgs: makeOverridable f (origArgs // (if isFunction newArgs then newArgs origArgs else newArgs))
      |             ^
   172|           );

… while calling a functor (an attribute set with a '__functor' attribute)
  at <nixpkgs> + /lib/customisation.nix:171:22:
   170|             */
   171|             newArgs: makeOverridable f (origArgs // (if isFunction newArgs then newArgs origArgs else newArgs))
      |                      ^
   172|           );

… from call site
  at <nixpkgs> + /lib/trivial.nix:1183:7:
  1182|     g: {
  1183|       __functor = self: g;
      |       ^
  1184|       __functionArgs = fArgs;

… while calling 'f''
  at <nixpkgs> + /lib/customisation.nix:158:9:
   157|       f' =
   158|         origArgs:
      |         ^
   159|         let

… while evaluating a branch condition
  at <nixpkgs> + /lib/customisation.nix:174:9:
   173|         in
   174|         if isAttrs result then
      |         ^
   175|           result

… while calling the 'isAttrs' builtin
  at <nixpkgs> + /lib/customisation.nix:174:12:
   173|         in
   174|         if isAttrs result then
      |            ^
   175|           result

… from call site
  at <nixpkgs> + /lib/customisation.nix:160:20:
   159|         let
   160|           result = f origArgs;
      |                    ^
   161|

… while calling anonymous lambda
  at <nixpkgs> + /pkgs/applications/networking/browsers/chromium/default.nix:1:1:
     1| {
      | ^
     2|   newScope,

… from call site
  at <nixpkgs> + /pkgs/applications/networking/browsers/chromium/default.nix:96:1:
    95| in
    96| stdenv.mkDerivation {
      | ^
    97|   pname = lib.optionalString ungoogled "ungoogled-" + "chromium";

… while calling 'mkDerivation'
  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:225:18:
   224|   */
   225|   mkDerivation = fnOrAttrs: makeDerivationExtensible (toFunction fnOrAttrs);
      |                  ^
   226|

… from call site
  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:225:29:
   224|   */
   225|   mkDerivation = fnOrAttrs: makeDerivationExtensible (toFunction fnOrAttrs);
      |                             ^
   226|

… while calling 'makeDerivationExtensible'
  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:229:5:
   228|   makeDerivationExtensible =
   229|     rattrs:
      |     ^
   230|     let

… from call site
  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:308:22:
   307|
   308|       finalPackage = mkDerivationSimple overrideAttrs args;
      |                      ^
   309|

… while calling 'mkDerivationSimple'
  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:925:5:
   924|     # * [package attributes]: https://nixos.org/manual/nix/stable/glossary#package-attribute-set
   925|     {
      |     ^
   926|

… from call site
  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:1015:5:
  1014|
  1015|     extendDerivation validity.handled (
      |     ^
  1016|       {

… while calling 'extendDerivation'
  at <nixpkgs> + /lib/customisation.nix:400:26:
   399|   extendDerivation =
   400|     condition: passthru: drv:
      |                          ^
   401|     let

… in the left operand of the update (//) operator
  at <nixpkgs> + /lib/customisation.nix:404:12:
   403|         drv
   404|         // listToAttrs (
      |            ^
   405|           outputsList

… while calling the 'listToAttrs' builtin
  at <nixpkgs> + /lib/customisation.nix:404:12:
   403|         drv
   404|         // listToAttrs (
      |            ^
   405|           outputsList

… while calling the 'map' builtin
  at <nixpkgs> + /lib/customisation.nix:423:21:
   422|
   423|       outputsList = map (outputName: {
      |                     ^
   424|         name = outputName;

… from call site
  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:1071:8:
  1070|         passthru
  1071|     ) (derivation (derivationArg // checkedEnv));
      |        ^
  1072|

… in the right operand of the update (//) operator
  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:1071:37:
  1070|         passthru
  1071|     ) (derivation (derivationArg // checkedEnv));
      |                                     ^
  1072|

… in the condition of the assert statement
  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:992:9:
   991|           || throw "`env` must be an attribute set of environment variables. Set `env.env` or pick a more specific name.";
   992|         assert
      |         ^
   993|           (overlappingArgs == { })

… in the left operand of the OR (||) operator
  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:994:11:
   993|           (overlappingArgs == { })
   994|           || throw (
      |           ^
   995|             let

… while calling the 'intersectAttrs' builtin
  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:987:29:
   986|         let
   987|           overlappingArgs = intersectAttrs env' derivationArg;
      |                             ^
   988|         in

… while evaluating a branch condition
  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:964:9:
   963|       env' =
   964|         if attrs ? meta.mainProgram then env // { NIX_MAIN_PROGRAM = attrs.meta.mainProgram; } else env;
      |         ^
   965|

… while evaluating the attribute 'browser.meta'
  at <nixpkgs> + /pkgs/applications/networking/browsers/chromium/default.nix:64:5:
    63|
    64|     browser = callPackage ./browser.nix {
      |     ^
    65|       inherit chromiumVersionAtLeast enableWideVine ungoogled;

… from call site
  at <nixpkgs> + /pkgs/applications/networking/browsers/chromium/default.nix:64:15:
    63|
    64|     browser = callPackage ./browser.nix {
      |               ^
    65|       inherit chromiumVersionAtLeast enableWideVine ungoogled;

(7 duplicate frames omitted)

… while calling anonymous lambda
  at <nixpkgs> + /pkgs/applications/networking/browsers/chromium/browser.nix:1:1:
     1| {
      | ^
     2|   lib,

… while calling a functor (an attribute set with a '__functor' attribute)
  at <nixpkgs> + /pkgs/applications/networking/browsers/chromium/browser.nix:9:1:
     8|
     9| mkChromiumDerivation (base: rec {
      | ^
    10|   name = "chromium-browser";

… from call site
  at <nixpkgs> + /lib/trivial.nix:1083:5:
  1082|     # TODO: Should we add call-time "type" checking like built in?
  1083|     __functor = self: f;
      |     ^
  1084|     __functionArgs = args;

… while calling anonymous lambda
  at <nixpkgs> + /pkgs/applications/networking/browsers/chromium/common.nix:104:1:
   103|
   104| buildFun:
      | ^
   105|

… from call site
  at <nixpkgs> + /pkgs/applications/networking/browsers/chromium/common.nix:1052:1:
  1051| # Remove some extraAttrs we supplied to the base attributes already.
  1052| stdenv.mkDerivation (
      | ^
  1053|   base

(12 duplicate frames omitted)

… in the condition of the assert statement
  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:989:9:
   988|         in
   989|         assert
      |         ^
   990|           (isAttrs env && !isDerivation env)

… in the left operand of the OR (||) operator
  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:991:11:
   990|           (isAttrs env && !isDerivation env)
   991|           || throw "`env` must be an attribute set of environment variables. Set `env.env` or pick a more specific name.";
      |           ^
   992|         assert

… in the left operand of the AND (&&) operator
  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:990:24:
   989|         assert
   990|           (isAttrs env && !isDerivation env)
      |                        ^
   991|           || throw "`env` must be an attribute set of environment variables. Set `env.env` or pick a more specific name.";

… while calling the 'isAttrs' builtin
  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:990:12:
   989|         assert
   990|           (isAttrs env && !isDerivation env)
      |            ^
   991|           || throw "`env` must be an attribute set of environment variables. Set `env.env` or pick a more specific name.";

… in the right operand of the update (//) operator
  at <nixpkgs> + /pkgs/applications/networking/browsers/chromium/common.nix:995:8:
   994|     }
   995|     // lib.optionalAttrs (chromiumVersionAtLeast "150") {
      |        ^
   996|       # [56385/56385] LINK ./chrome

… from call site
  at <nixpkgs> + /pkgs/applications/networking/browsers/chromium/common.nix:995:8:
   994|     }
   995|     // lib.optionalAttrs (chromiumVersionAtLeast "150") {
      |        ^
   996|       # [56385/56385] LINK ./chrome

… while calling 'optionalAttrs'
  at <nixpkgs> + /lib/attrsets.nix:1482:25:
  1481|   */
  1482|   optionalAttrs = cond: as: if cond then as else { };
      |                         ^
  1483|

… while evaluating a branch condition
  at <nixpkgs> + /lib/attrsets.nix:1482:29:
  1481|   */
  1482|   optionalAttrs = cond: as: if cond then as else { };
      |                             ^
  1483|

… from call site
  at <nixpkgs> + /pkgs/applications/networking/browsers/chromium/common.nix:995:27:
   994|     }
   995|     // lib.optionalAttrs (chromiumVersionAtLeast "150") {
      |                           ^
   996|       # [56385/56385] LINK ./chrome

… while calling 'chromiumVersionAtLeast'
  at <nixpkgs> + /pkgs/applications/networking/browsers/chromium/default.nix:43:28:
    42|   # Helper functions for changes that depend on specific versions:
    43|   chromiumVersionAtLeast = min-version: lib.versionAtLeast upstream-info.version min-version;
      |                            ^
    44|   versionRange =

… from call site
  at <nixpkgs> + /pkgs/applications/networking/browsers/chromium/default.nix:43:41:
    42|   # Helper functions for changes that depend on specific versions:
    43|   chromiumVersionAtLeast = min-version: lib.versionAtLeast upstream-info.version min-version;
      |                                         ^
    44|   versionRange =

… while calling 'versionAtLeast'
  at <nixpkgs> + /lib/strings.nix:2002:24:
  2001|   */
  2002|   versionAtLeast = v1: v2: compareVersions v2 v1 != 1;
      |                        ^
  2003|

… while calling the 'compareVersions' builtin
  at <nixpkgs> + /lib/strings.nix:2002:28:
  2001|   */
  2002|   versionAtLeast = v1: v2: compareVersions v2 v1 != 1;
      |                            ^
  2003|

… while evaluating the second argument passed to builtins.compareVersions

… while evaluating the attribute 'chromiumInfo'
  at ./by-name/br/brave-origin/source.nix:165:5:
   164|     lock
   165|     chromiumInfo
      |     ^
   166|     braveOverlayEntries

… from call site
  at ./by-name/br/brave-origin/source.nix:16:8:
    15|     else
    16|       (lib.importJSON "${pkgs.path}/pkgs/applications/networking/browsers/chromium/info.json").chromium;
      |        ^
    17|

… while calling 'importJSON'
  at <nixpkgs> + /lib/trivial.nix:794:16:
   793|   */
   794|   importJSON = path: fromJSON (readFile path);
      |                ^
   795|

… while calling the 'fromJSON' builtin
  at <nixpkgs> + /lib/trivial.nix:794:22:
   793|   */
   794|   importJSON = path: fromJSON (readFile path);
      |                      ^
   795|

… while evaluating the first argument passed to builtins.fromJSON

… while calling the 'readFile' builtin
  at <nixpkgs> + /lib/trivial.nix:794:32:
   793|   */
   794|   importJSON = path: fromJSON (readFile path);
      |                                ^
   795|

… while realising the context of path '/nix/store/vq25n5mn6af497ygh993snf5d34vvnds-grir922ynsi1acqkrxm6ijxfp5xnwdha-source/pkgs/applications/networking/browsers/chromium/info.json'

error: path '/nix/store/vq25n5mn6af497ygh993snf5d34vvnds-grir922ynsi1acqkrxm6ijxfp5xnwdha-source' is not valid
