trace: evaluation warning: thunderbird-unwrapped-140.7.0esr was overridden with `version` but not `src` at ./pkgs/betterbird/default.nix:21:9.

This is most likely not what you want. In order to properly change the version of a package, override
both the `version` and `src` attributes:

hello.overrideAttrs (oldAttrs: rec {
  version = "1.0.0";
  src = pkgs.fetchurl {
    url = "mirror://gnu/hello/hello-${version}.tar.gz";
    hash = "...";
  };
})

(To silence this warning, set `__intentionallyOverridingVersion = true` in your `overrideAttrs` call.)

error:
… while querying the derivation named 'plezy-1.15.0'

… while calling the 'foldl'' builtin

  at <nixpkgs> + /pkgs/stdenv/generic/check-meta.nix:628:21:

   627|       # if you add a new maintainer or team attribute please ensure that this expectation is still met.
   628|       maintainers = unique (
      |                     ^
   629|         attrs.meta.maintainers or [ ] ++ concatMap (team: team.members or [ ]) attrs.meta.teams or [ ]

… while calling anonymous lambda

  at <nixpkgs> + /lib/lists.nix:1859:25:

  1858|   */
  1859|   unique = foldl' (acc: e: if elem e acc then acc else acc ++ [ e ]) [ ];
      |                         ^
  1860|

error: undefined variable 'miniharinn'

at ./by-name/plezy/package.nix:159:7:

   158|       mio
   159|       miniharinn
      |       ^
   160|     ];
