error:
… from call site

  at ./packages.nix:148:16:

   147|   betterbird-unwrapped = v3overrideAttrs (pkgs.callPackage ./pkgs/betterbird { });
   148|   betterbird = pkgs.wrapThunderbird betterbird-unwrapped {
      |                ^
   149|     applicationName = "betterbird";

… while calling anonymous lambda

  at <nixpkgs> + /pkgs/applications/networking/mailreaders/thunderbird/wrapper.nix:8:10:

     7|
     8| browser: args:
      |          ^
     9|

… from call site

  at <nixpkgs> + /lib/trivial.nix:1017:5:

  1016|     # TODO: Should we add call-time "type" checking like built in?
  1017|     __functor = self: f;
      |     ^
  1018|     __functionArgs = args;

… while calling anonymous lambda

  at <nixpkgs> + /lib/customisation.nix:178:7:

   177|     decorate (
   178|       origArgs:
      |       ^
   179|       let

… from call site

  at <nixpkgs> + /lib/customisation.nix:180:18:

   179|       let
   180|         result = f origArgs;
      |                  ^
   181|

… while calling 'wrapper'

  at <nixpkgs> + /pkgs/applications/networking/browsers/firefox/wrapper.nix:56:5:

    55|   wrapper =
    56|     {
      |     ^
    57|       applicationName ? browser.binaryName or (lib.getName browser), # Note: this is actually *binary* name and is different from browser.applicationName, which is *app* name!

… from call site

  at <nixpkgs> + /pkgs/applications/networking/browsers/firefox/wrapper.nix:214:5:

   213|     in
   214|     stdenv.mkDerivation (finalAttrs: {
      |     ^
   215|       __structuredAttrs = true;

… while calling 'mkDerivation'

  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:66:18:

    65|   */
    66|   mkDerivation = fnOrAttrs: makeDerivationExtensible (toFunction fnOrAttrs);
      |                  ^
    67|

… from call site

  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:66:29:

    65|   */
    66|   mkDerivation = fnOrAttrs: makeDerivationExtensible (toFunction fnOrAttrs);
      |                             ^
    67|

… while calling 'makeDerivationExtensible'

  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:77:5:

    76|   makeDerivationExtensible =
    77|     rattrs:
      |     ^
    78|     let

… from call site

  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:139:22:

   138|
   139|       finalPackage = mkDerivationSimple overrideAttrs args;
      |                      ^
   140|

… while calling 'mkDerivationSimple'

  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:772:5:

   771|     # * [package attributes]: https://nixos.org/manual/nix/stable/glossary#package-attribute-set
   772|     {
      |     ^
   773|

… from call site

  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:882:5:

   881|
   882|     extendDerivation validity.handled (
      |     ^
   883|       {

… while calling 'extendDerivation'

  at <nixpkgs> + /lib/customisation.nix:411:26:

   410|   extendDerivation =
   411|     condition: passthru: drv:
      |                          ^
   412|     let

… from call site

  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:850:16:

   849|           "`env` must be an attribute set of environment variables. Set `env.env` or pick a more specific name.";
   850|         assert assertMsg (overlappingNames == [ ])
      |                ^
   851|           "The `env` attribute set cannot contain any attributes passed to derivation. The following attributes are overlapping:\n${errors}";

… while calling 'assertMsg'

  at <nixpkgs> + /lib/asserts.nix:50:21:

    49|   # TODO(Profpatsch): add tests that check stderr
    50|   assertMsg = pred: msg: pred || throw msg;
      |                     ^
    51|

… from call site

  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:811:21:

   810|       mainProgram = meta.mainProgram or null;
   811|       env' = env // lib.optionalAttrs (mainProgram != null) { NIX_MAIN_PROGRAM = mainProgram; };
      |                     ^
   812|

… while calling 'optionalAttrs'

  at <nixpkgs> + /lib/attrsets.nix:1478:25:

  1477|   */
  1478|   optionalAttrs = cond: as: if cond then as else { };
      |                         ^
  1479|

… from call site

  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:827:14:

   826|
   827|       meta = checkMeta.commonMeta {
      |              ^
   828|         inherit validity attrs pos;

… while calling 'commonMeta'

  at <nixpkgs> + /pkgs/stdenv/generic/check-meta.nix:572:5:

   571|   commonMeta =
   572|     {
      |     ^
   573|       validity,

… from call site

  at ./packages.nix:147:26:

   146|   firefox_nightly = pkgs.wrapFirefox firefox_nightly-unwrapped { };
   147|   betterbird-unwrapped = v3overrideAttrs (pkgs.callPackage ./pkgs/betterbird { });
      |                          ^
   148|   betterbird = pkgs.wrapThunderbird betterbird-unwrapped {

… while calling anonymous lambda

  at ./private.nix:29:7:

    28|     if pkgs.stdenv.hostPlatform.isx86_64 then
    29|       x:
      |       ^
    30|       x.overrideAttrs (old: {

… from call site

  at ./packages.nix:147:43:

   146|   firefox_nightly = pkgs.wrapFirefox firefox_nightly-unwrapped { };
   147|   betterbird-unwrapped = v3overrideAttrs (pkgs.callPackage ./pkgs/betterbird { });
      |                                           ^
   148|   betterbird = pkgs.wrapThunderbird betterbird-unwrapped {

… while calling 'callPackageWith'

  at <nixpkgs> + /lib/customisation.nix:276:19:

   275|   callPackageWith =
   276|     autoArgs: fn: args:
      |                   ^
   277|     let

… from call site

  at <nixpkgs> + /lib/trivial.nix:1017:5:

  1016|     # TODO: Should we add call-time "type" checking like built in?
  1017|     __functor = self: f;
      |     ^
  1018|     __functionArgs = args;

… while calling anonymous lambda

  at <nixpkgs> + /lib/customisation.nix:178:7:

   177|     decorate (
   178|       origArgs:
      |       ^
   179|       let

… from call site

  at <nixpkgs> + /lib/customisation.nix:180:18:

   179|       let
   180|         result = f origArgs;
      |                  ^
   181|

… while calling anonymous lambda

  at ./pkgs/betterbird/default.nix:1:1:

     1| {
      | ^
     2|   lib,

… from call site

  at <nixpkgs> + /lib/trivial.nix:1017:5:

  1016|     # TODO: Should we add call-time "type" checking like built in?
  1017|     __functor = self: f;
      |     ^
  1018|     __functionArgs = args;

… while calling anonymous lambda

  at <nixpkgs> + /lib/customisation.nix:194:11:

   193|           */
   194|           newArgs: makeOverridable f (overrideWith newArgs)
      |           ^
   195|         );

… from call site

  at <nixpkgs> + /lib/trivial.nix:1017:5:

  1016|     # TODO: Should we add call-time "type" checking like built in?
  1017|     __functor = self: f;
      |     ^
  1018|     __functionArgs = args;

… while calling anonymous lambda

  at <nixpkgs> + /lib/customisation.nix:178:7:

   177|     decorate (
   178|       origArgs:
      |       ^
   179|       let

error: function 'anonymous lambda' called with unexpected argument 'icu73'

at <nixpkgs> + /pkgs/build-support/build-mozilla-mach/default.nix:43:1:

    42|
    43| {
      | ^
    44|   lib,
Did you mean one of icu77 or icu78?
