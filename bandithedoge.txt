error:
… from call site
  at <nixpkgs> + /lib/attrsets.nix:1724:11:
  1723|         zipAttrsWith (
  1724|           name: values:
      |           ^
  1725|           let

… while calling anonymous lambda
  at <nixpkgs> + /lib/attrsets.nix:1724:17:
  1723|         zipAttrsWith (
  1724|           name: values:
      |                 ^
  1725|           let

… while calling the 'head' builtin
  at <nixpkgs> + /lib/attrsets.nix:1729:13:
  1728|           if length values == 1 || pred here (elemAt values 1) (head values) then
  1729|             head values
      |             ^
  1730|           else

… while calling anonymous lambda
  at <nixpkgs> + /lib/attrsets.nix:1724:17:
  1723|         zipAttrsWith (
  1724|           name: values:
      |                 ^
  1725|           let

… while calling the 'head' builtin
  at <nixpkgs> + /lib/attrsets.nix:1729:13:
  1728|           if length values == 1 || pred here (elemAt values 1) (head values) then
  1729|             head values
      |             ^
  1730|           else

… while calling anonymous lambda
  at <nixpkgs> + /lib/attrsets.nix:1724:17:
  1723|         zipAttrsWith (
  1724|           name: values:
      |                 ^
  1725|           let

… while calling the 'head' builtin
  at <nixpkgs> + /lib/attrsets.nix:1729:13:
  1728|           if length values == 1 || pred here (elemAt values 1) (head values) then
  1729|             head values
      |             ^
  1730|           else

… while calling anonymous lambda
  at <nixpkgs> + /lib/attrsets.nix:1195:17:
  1194|         mapAttrs (
  1195|           name: value:
      |                 ^
  1196|           if isAttrs value && cond value then recurse (path ++ [ name ]) value else f (path ++ [ name ]) value

… from call site
  at <nixpkgs> + /lib/attrsets.nix:1196:85:
  1195|           name: value:
  1196|           if isAttrs value && cond value then recurse (path ++ [ name ]) value else f (path ++ [ name ]) value
      |                                                                                     ^
  1197|         );

… while calling anonymous lambda
  at ./default.nix:12:10:
    11|   (pkgs.lib.mapAttrsRecursive (
    12|     old: new:
      |          ^
    13|     pkgs.lib.warn "${concat old} has been renamed to ${concat new}" (pkgs.lib.attrByPath new null all)

… while calling the 'warn' builtin
  at ./default.nix:13:5:
    12|     old: new:
    13|     pkgs.lib.warn "${concat old} has been renamed to ${concat new}" (pkgs.lib.attrByPath new null all)
      |     ^
    14|   ) (pkgs.callPackage ./_renamed.nix { }))

… while evaluating the first argument; the message passed to builtins.warn

… while calling the 'concatStringsSep' builtin
  at ./default.nix:13:56:
    12|     old: new:
    13|     pkgs.lib.warn "${concat old} has been renamed to ${concat new}" (pkgs.lib.attrByPath new null all)
      |                                                        ^
    14|   ) (pkgs.callPackage ./_renamed.nix { }))

… while evaluating the second argument (the list of strings to concat) passed to builtins.concatStringsSep

error: expected a list but found a function: «lambda overrideDerivation @ <nixpkgs> + /lib/customisation.nix:180:15»
