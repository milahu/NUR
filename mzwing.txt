error:
… while evaluating the attribute 'cliproxyapiplus'
  at ./internal/discover.nix:57:7:
    56|       inherit name;
    57|       value = pkgs.callPackage (dir + "/${name}/default.nix") (
      |       ^
    58|         autoArgsFor name

… from call site
  at ./internal/discover.nix:57:15:
    56|       inherit name;
    57|       value = pkgs.callPackage (dir + "/${name}/default.nix") (
      |               ^
    58|         autoArgsFor name

… while calling anonymous lambda
  at <nixpkgs> + /lib/customisation.nix:308:19:
   307|     in
   308|     autoArgs: fn: args:
      |                   ^
   309|     let

… while calling a functor (an attribute set with a '__functor' attribute)
  at <nixpkgs> + /lib/customisation.nix:323:7:
   322|     if unpassedArgs == { } || all (value: value) (attrValues unpassedArgs) then
   323|       makeOverridable f allArgs
      |       ^
   324|     else

… from call site
  at <nixpkgs> + /lib/trivial.nix:1183:7:
  1182|     g: {
  1183|       __functor = self: g;
      |       ^
  1184|       __functionArgs = fArgs;

… while calling 'f''
  at <nixpkgs> + /lib/customisation.nix:159:9:
   158|       f' =
   159|         origArgs:
      |         ^
   160|         let

… while evaluating a branch condition
  at <nixpkgs> + /lib/customisation.nix:175:9:
   174|         in
   175|         if isAttrs result then
      |         ^
   176|           result

… while calling the 'isAttrs' builtin
  at <nixpkgs> + /lib/customisation.nix:175:12:
   174|         in
   175|         if isAttrs result then
      |            ^
   176|           result

… from call site
  at <nixpkgs> + /lib/customisation.nix:161:20:
   160|         let
   161|           result = f origArgs;
      |                    ^
   162|

… while calling anonymous lambda
  at ./pkgs/cliproxyapiplus/default.nix:1:1:
     1| {
      | ^
     2|   lib,

… from call site
  at ./internal/discover.nix:52:17:
    51|         name = arg;
    52|         value = valueFor arg;
      |                 ^
    53|       }) (builtins.filter shouldInject (builtins.attrNames fargs)));

… while calling 'valueFor'
  at ./internal/discover.nix:39:18:
    38|         || builtins.hasAttr arg inject;
    39|       valueFor = arg:
      |                  ^
    40|         if arg == "source" && builtins.hasAttr name sources

… while calling the 'getAttr' builtin
  at ./internal/discover.nix:48:14:
    47|         then builtins.getAttr arg sources
    48|         else builtins.getAttr arg inject;
      |              ^
    49|     in

… from call site
  at ./internal/discover.nix:8:16:
     7|   defaultInject = pkgs: let
     8|     overlaid = pkgs.extend (import ((import ./gomod2nix.nix) + "/overlay.nix"));
      |                ^
     9|   in {

… while calling 'extend'
  at <nixpkgs> + /pkgs/top-level/stage.nix:281:14:
   280|     # Prefer appendOverlays if used repeatedly.
   281|     extend = f: self.appendOverlays [ f ];
      |              ^
   282|

… from call site
  at <nixpkgs> + /pkgs/top-level/stage.nix:281:17:
   280|     # Prefer appendOverlays if used repeatedly.
   281|     extend = f: self.appendOverlays [ f ];
      |                 ^
   282|

… while calling 'appendOverlays'
  at <nixpkgs> + /pkgs/top-level/stage.nix:268:7:
   267|     appendOverlays =
   268|       extraOverlays:
      |       ^
   269|       if extraOverlays == [ ] then

… from call site
  at <nixpkgs> + /pkgs/top-level/stage.nix:272:9:
   271|       else
   272|         nixpkgsFun { overlays = args.overlays ++ extraOverlays; };
      |         ^
   273|

… while calling 'nixpkgsFun'
  at <nixpkgs> + /pkgs/top-level/default.nix:195:16:
   194|   # experience here.)
   195|   nixpkgsFun = newArgs: import ./. (args // newArgs);
      |                ^
   196|

… from call site
  at <nixpkgs> + /pkgs/top-level/default.nix:195:25:
   194|   # experience here.)
   195|   nixpkgsFun = newArgs: import ./. (args // newArgs);
      |                         ^
   196|

… while calling anonymous lambda
  at <nixpkgs> + /pkgs/top-level/default.nix:21:1:
    20|
    21| {
      | ^
    22|   # The system packages will be built on. See the manual for the

… from call site
  at <nixpkgs> + /pkgs/top-level/default.nix:108:6:
   107|   checked =
   108|     (throwIfNot (lib.isList overlays) "The overlays argument to nixpkgs must be a list.")
      |      ^
   109|       (throwIfNot (lib.all lib.isFunction overlays) "All overlays passed to nixpkgs must be functions.")

… while calling anonymous lambda
  at <nixpkgs> + /lib/trivial.nix:982:40:
   981|   */
   982|   throwIfNot = cond: msg: if cond then x: x else throw msg;
      |                                        ^
   983|

… from call site
  at <nixpkgs> + /pkgs/top-level/default.nix:109:8:
   108|     (throwIfNot (lib.isList overlays) "The overlays argument to nixpkgs must be a list.")
   109|       (throwIfNot (lib.all lib.isFunction overlays) "All overlays passed to nixpkgs must be functions.")
      |        ^
   110|       (throwIfNot (lib.isList crossOverlays) "The crossOverlays argument to nixpkgs must be a list.")

… while calling 'throwIfNot'
  at <nixpkgs> + /lib/trivial.nix:982:22:
   981|   */
   982|   throwIfNot = cond: msg: if cond then x: x else throw msg;
      |                      ^
   983|

… while evaluating a branch condition
  at <nixpkgs> + /lib/trivial.nix:982:27:
   981|   */
   982|   throwIfNot = cond: msg: if cond then x: x else throw msg;
      |                           ^
   983|

… while calling the 'all' builtin
  at <nixpkgs> + /pkgs/top-level/default.nix:109:20:
   108|     (throwIfNot (lib.isList overlays) "The overlays argument to nixpkgs must be a list.")
   109|       (throwIfNot (lib.all lib.isFunction overlays) "All overlays passed to nixpkgs must be functions.")
      |                    ^
   110|       (throwIfNot (lib.isList crossOverlays) "The crossOverlays argument to nixpkgs must be a list.")

… while calling anonymous lambda
  at <nixpkgs> + /lib/trivial.nix:1131:5:
  1130|     in
  1131|     f: isFunction f || (f ? __functor && isFunction (f.__functor f));
      |     ^
  1132|

… in the left operand of the OR (||) operator
  at <nixpkgs> + /lib/trivial.nix:1131:21:
  1130|     in
  1131|     f: isFunction f || (f ? __functor && isFunction (f.__functor f));
      |                     ^
  1132|

… while calling the 'isFunction' builtin
  at <nixpkgs> + /lib/trivial.nix:1131:8:
  1130|     in
  1131|     f: isFunction f || (f ? __functor && isFunction (f.__functor f));
      |        ^
  1132|

… while calling the 'import' builtin
  at ./internal/discover.nix:8:29:
     7|   defaultInject = pkgs: let
     8|     overlaid = pkgs.extend (import ((import ./gomod2nix.nix) + "/overlay.nix"));
      |                             ^
     9|   in {

… while realising the context of a path

… while calling the 'import' builtin
  at ./internal/discover.nix:8:38:
     7|   defaultInject = pkgs: let
     8|     overlaid = pkgs.extend (import ((import ./gomod2nix.nix) + "/overlay.nix"));
      |                                      ^
     9|   in {

… while evaluating the file './internal/gomod2nix.nix':

… while calling the 'fetchTree' builtin
  at ./internal/gomod2nix.nix:5:3:
     4| in
     5|   builtins.fetchTree {
      |   ^
     6|     type = "github";

error: access to URI 'github:mzwing/gomod2nix/89b9a05a88f720ce9af82430e8f0b99cf4ddeee0?narHash=sha256-2F7Da0CpU8F5Le8%2BU2fLpSx9NNji6A%2BP%2Bl9pQbZ1eis%3D' is forbidden in restricted mode
