error:
… while evaluating the attribute 'agent-run'
  at ./default.nix:31:3:
    30|   cursor-cli-bin = callPackage ./pkgs/cursor-cli {};
    31|   agent-run = callPackage ./pkgs/agent-run.nix {};
      |   ^
    32|   create-tauri-app = callPackage ./pkgs/create-tauri-app {};

… while evaluating the 'name' attribute of a derivation

… from call site
  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:682:13:
   681|             in
   682|             sanitizeDerivationName (
      |             ^
   683|               if attrs ? name then

… while calling anonymous lambda
  at <nixpkgs> + /lib/strings.nix:2901:5:
  2900|     in
  2901|     string:
      |     ^
  2902|     # First detect the common case of already valid strings, to speed those up

… while evaluating a branch condition
  at <nixpkgs> + /lib/strings.nix:2903:5:
  2902|     # First detect the common case of already valid strings, to speed those up
  2903|     if stringLength string <= 207 && okRegex string != null then
      |     ^
  2904|       unsafeDiscardStringContext string

… in the left operand of the AND (&&) operator
  at <nixpkgs> + /lib/strings.nix:2903:35:
  2902|     # First detect the common case of already valid strings, to speed those up
  2903|     if stringLength string <= 207 && okRegex string != null then
      |                                   ^
  2904|       unsafeDiscardStringContext string

… in the argument of the not operator
  at <nixpkgs> + /lib/strings.nix:2903:28:
  2902|     # First detect the common case of already valid strings, to speed those up
  2903|     if stringLength string <= 207 && okRegex string != null then
      |                            ^
  2904|       unsafeDiscardStringContext string

… while calling the 'lessThan' builtin
  at <nixpkgs> + /lib/strings.nix:2903:28:
  2902|     # First detect the common case of already valid strings, to speed those up
  2903|     if stringLength string <= 207 && okRegex string != null then
      |                            ^
  2904|       unsafeDiscardStringContext string

… while calling the 'stringLength' builtin
  at <nixpkgs> + /lib/strings.nix:2903:8:
  2902|     # First detect the common case of already valid strings, to speed those up
  2903|     if stringLength string <= 207 && okRegex string != null then
      |        ^
  2904|       unsafeDiscardStringContext string

… in the condition of the assert statement
  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:687:17:
   686|                 # we cannot coerce null to a string below
   687|                 assert
      |                 ^
   688|                   (attrs ? version && attrs.version != null) || throw "The `version` attribute cannot be null.";

… in the left operand of the OR (||) operator
  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:688:62:
   687|                 assert
   688|                   (attrs ? version && attrs.version != null) || throw "The `version` attribute cannot be null.";
      |                                                              ^
   689|                 "${attrs.pname}${staticMarker}${hostSuffix}-${attrs.version}"

… in the right operand of the AND (&&) operator
  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:688:36:
   687|                 assert
   688|                   (attrs ? version && attrs.version != null) || throw "The `version` attribute cannot be null.";
      |                                    ^
   689|                 "${attrs.pname}${staticMarker}${hostSuffix}-${attrs.version}"

… while evaluating the attribute 'version'
  at ./pkgs/agent-run.nix:19:3:
    18|   pname = "agent-run";
    19|   version = "${agentRunSource.date}-${shortCommit}";
      |   ^
    20|

error: attribute 'agent-run' missing
at ./pkgs/agent-run.nix:9:20:
     8| let
     9|   agentRunSource = sources.agent-run;
      |                    ^
    10|   shortCommit = lib.substring 0 7 agentRunSource.version;
