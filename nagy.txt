error:
… from call site

  at <nixpkgs> + /lib/filesystem.nix:406:47:

   405|               # call .nix files
   406|               "${removeSuffix ".nix" name}" = callPackage path { };
      |                                               ^
   407|             }

… while calling 'callPackageWith'

  at <nixpkgs> + /lib/customisation.nix:276:19:

   275|   callPackageWith =
   276|     autoArgs: fn: args:
      |                   ^
   277|     let

error: evaluation aborted with the following error message: 'lib.customisation.callPackageWith: Function called without required argument "nur" at ./pkgs/by-name/passage-with-otp.nix:3, did you mean "nrr", "numr" or "nurl"?'
