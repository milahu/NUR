error:
… while evaluating attribute 'cargoDeps' of derivation 'velaterm-0.1.100'
  at ./pkgs/velaterm/default.nix:29:3:
    28|
    29|   cargoDeps = rustPlatform.importCargoLock {
      |   ^
    30|     lockFile = "${finalAttrs.src}/src-tauri/Cargo.lock";

… while calling the 'derivationStrict' builtin
  at «nix-internal»/derivation-internal.nix:37:12:
    36|
    37|   strict = derivationStrict drvAttrs;
      |            ^
    38|

… while evaluating derivation 'cargo-vendor-dir'
  whose name attribute is located at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:651:11

… while evaluating attribute 'buildCommand' of derivation 'cargo-vendor-dir'
  at <nixpkgs> + /pkgs/build-support/trivial-builders/default.nix:84:17:
    83|         enableParallelBuilding = true;
    84|         inherit buildCommand name;
      |                 ^
    85|         passAsFile = defaultPassAsFile ++ (derivationArgs.passAsFile or [ ]);

… while calling the 'toString' builtin
  at <nixpkgs> + /pkgs/build-support/rust/import-cargo-lock.nix:340:28:
   339|
   340|             for crate in ${toString depCrates}; do
      |                            ^
   341|               # Link the crate directory, removing the output path hash from the destination.

… while calling the 'deepSeq' builtin
  at <nixpkgs> + /pkgs/build-support/rust/import-cargo-lock.nix:125:15:
   124|   # being evaluated otherwise, since there could be no git dependencies.
   125|   depCrates = deepSeq gitShaOutputHash (map mkCrate depPackages);
      |               ^
   126|

… while calling the 'map' builtin
  at <nixpkgs> + /pkgs/build-support/rust/import-cargo-lock.nix:125:41:
   124|   # being evaluated otherwise, since there could be no git dependencies.
   125|   depCrates = deepSeq gitShaOutputHash (map mkCrate depPackages);
      |                                         ^
   126|

… while calling the 'filter' builtin
  at <nixpkgs> + /pkgs/build-support/rust/import-cargo-lock.nix:118:17:
   117|   # safely skip it.
   118|   depPackages = filter (p: p ? "source") packages;
      |                 ^
   119|

… while calling the 'fromTOML' builtin
  at <nixpkgs> + /pkgs/build-support/rust/import-cargo-lock.nix:107:20:
   106|
   107|   parsedLockFile = fromTOML lockFileContents;
      |                    ^
   108|

… while evaluating the argument passed to builtins.fromTOML

… while calling the 'readFile' builtin
  at <nixpkgs> + /pkgs/build-support/rust/import-cargo-lock.nix:105:47:
   104|   # shadows args.lockFileContents
   105|   lockFileContents = if lockFile != null then readFile lockFile else args.lockFileContents;
      |                                               ^
   106|

… while realising the context of path '/nix/store/80giwrpkbm2md5m06pknisw0pvmsc8ng-source/src-tauri/Cargo.lock'

error: path '/nix/store/dzbydqrq3qd22izspbq9drl0c0gd7lwj-source.drv' is not valid
