error:
… while evaluating the attribute 'tsodingPackages'
  at <nixpkgs> + /lib/attrsets.nix:987:47:
   986|   */
   987|   nameValuePair = name: value: { inherit name value; };
      |                                               ^
   988|

… while evaluating the attribute 'boomer'
  at <nixpkgs> + /lib/attrsets.nix:987:47:
   986|   */
   987|   nameValuePair = name: value: { inherit name value; };
      |                                               ^
   988|

… from call site
  at <nixpkgs> + /lib/filesystem.nix:393:27:
   392|               # recurse into directories
   393|               "${name}" = packagesFromDirectoryRecursive (
      |                           ^
   394|                 args

… while calling anonymous lambda
  at <nixpkgs> + /lib/filesystem.nix:415:5:
   414|     in
   415|     {
      |     ^
   416|       callPackage,

… from call site
  at <nixpkgs> + /lib/filesystem.nix:425:7:
   424|       # if `${directory}/package.nix` exists, call it directly
   425|       callPackage defaultPath { }
      |       ^
   426|     else if args ? newScope then

… while calling anonymous lambda
  at <nixpkgs> + /lib/customisation.nix:307:19:
   306|     in
   307|     autoArgs: fn: args:
      |                   ^
   308|     let

… while calling the 'abort' builtin
  at <nixpkgs> + /lib/customisation.nix:329:7:
   328|       # which is especially relevant with allowAliases = false
   329|       abort (makeErrorMessage autoArgs fn args fargs unpassedArgs);
      |       ^
   330|

error: evaluation aborted with the following error message: 'lib.customisation.callPackageWith: Function called without required argument "nim-1_0" at ./pkgs/tsodingPackages/boomer/package.nix:8, did you mean "nim_1_0" or "nim_2_0"?'
