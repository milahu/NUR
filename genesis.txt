error:
… from call site

  at ./default.nix:29:25:

    28|   clocktimer = pkgs.callPackage ./pkgs/clocktimer { lib = mylib; };
    29|   fragments-of-euclid = pkgs.callPackage ./pkgs/fragments-of-euclid { lib = mylib; };
      |                         ^
    30|   freediag = pkgs.callPackage ./pkgs/freediag { lib = mylib; };

… while calling 'callPackageWith'

  at <nixpkgs> + /lib/customisation.nix:212:35:

   211|   */
   212|   callPackageWith = autoArgs: fn: args:
      |                                   ^
   213|     let

error: evaluation aborted with the following error message: 'lib.customisation.callPackageWith: Function called without required argument "gdk_pixbuf" at ./pkgs/fragments-of-euclid/default.nix:9, did you mean "gdk-pixbuf"?'
