trace: evaluation warning: 'makeOverridable' has been removed from pkgs, use `lib.makeOverridable` instead
error:
… while querying the derivation named 'rust-cbindgen-0.14.3'

… in the left operand of the AND (&&) operator

  at <nixpkgs> + /pkgs/stdenv/generic/check-meta.nix:645:9:

   644|         validity.valid != "no"
   645|         && ((config.checkMetaRecursively or false) -> all (d: d.meta.available or true) references);
      |         ^
   646|     };

… from call site

  at <nixpkgs> + /pkgs/stdenv/generic/make-derivation.nix:833:18:

   832|       };
   833|       validity = checkMeta.assertValidity { inherit meta attrs; };
      |                  ^
   834|

… while calling 'assertValidity'

  at <nixpkgs> + /pkgs/stdenv/generic/check-meta.nix:686:5:

   685|   assertValidity =
   686|     { meta, attrs }:
      |     ^
   687|     let

… from call site

  at <nixpkgs> + /pkgs/stdenv/generic/check-meta.nix:688:17:

   687|     let
   688|       invalid = checkValidity attrs;
      |                 ^
   689|       problems = checkProblems attrs;

… while calling 'checkValidity'

  at <nixpkgs> + /pkgs/stdenv/generic/check-meta.nix:417:5:

   416|   checkValidity =
   417|     attrs:
      |     ^
   418|     # Check meta attribute types first, to make sure it is always called even when there are other issues

… from call site

  at <nixpkgs> + /pkgs/stdenv/generic/check-meta.nix:462:13:

   461|       }
   462|     else if hasUnsupportedPlatform attrs && !allowUnsupportedSystem then
      |             ^
   463|       let

… while calling 'hasUnsupportedPlatform'

  at <nixpkgs> + /pkgs/stdenv/generic/check-meta.nix:136:28:

   135|
   136|   hasUnsupportedPlatform = pkg: !(availableOn hostPlatform pkg);
      |                            ^
   137|

… from call site

  at <nixpkgs> + /pkgs/stdenv/generic/check-meta.nix:136:35:

   135|
   136|   hasUnsupportedPlatform = pkg: !(availableOn hostPlatform pkg);
      |                                   ^
   137|

… while calling 'availableOn'

  at <nixpkgs> + /lib/meta.nix:369:15:

   368|   availableOn =
   369|     platform: pkg:
      |               ^
   370|     ((!pkg ? meta.platforms) || any (platformMatch platform) pkg.meta.platforms)

error: attribute 'targetPlatforms' missing

at <nixpkgs> + /pkgs/build-support/rust/build-rust-package/default.nix:181:76:

   180|         # default to Rust's platforms
   181|         platforms = lib.intersectLists meta.platforms or lib.platforms.all rustc.targetPlatforms;
      |                                                                            ^
   182|       };
