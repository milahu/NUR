error:
… from call site

  at <nixpkgs> + /lib/filesystem.nix:384:27:

   383|               # recurse into directories
   384|               "${name}" = packagesFromDirectoryRecursive (
      |                           ^
   385|                 args

… while calling anonymous lambda

  at <nixpkgs> + /lib/filesystem.nix:406:5:

   405|     in
   406|     {
      |     ^
   407|       callPackage,

… from call site

  at <nixpkgs> + /lib/filesystem.nix:416:7:

   415|       # if `${directory}/package.nix` exists, call it directly
   416|       callPackage defaultPath { }
      |       ^
   417|     else if args ? newScope then

… while calling 'callPackageWith'

  at <nixpkgs> + /lib/customisation.nix:259:19:

   258|   callPackageWith =
   259|     autoArgs: fn: args:
      |                   ^
   260|     let

error: evaluation aborted with the following error message: 'lib.customisation.callPackageWith: Function called without required argument "yacc" at ./pkgs/by-name/crust-firmware-pinephone/package.nix:8, did you mean "acc", "byacc" or "sacc"?'
