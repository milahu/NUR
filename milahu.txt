trace: evaluation warning: fontforge-20251009 was overridden with `version` but not `src` at ./default.nix:864:5.

This is most likely not what you want. In order to properly change the version of a package, override
both the `version` and `src` attributes:

hello.overrideAttrs (oldAttrs: rec {
  version = "1.0.0";
  src = pkgs.fetchurl {
    url = "mirror://gnu/hello/hello-${version}.tar.gz";
    hash = "...";
  };
})

(To silence this warning, set `__intentionallyOverridingVersion = true` in your `overrideAttrs` call.)

trace: evaluation warning: dontRecurseIntoAttrs has been removed from pkgs, use `lib.dontRecurseIntoAttrs` instead
trace: evaluation warning: dontRecurseIntoAttrs has been removed from pkgs, use `lib.dontRecurseIntoAttrs` instead
trace: evaluation warning: 'makeOverridable' has been removed from pkgs, use `lib.makeOverridable` instead
trace: evaluation warning: wine-11.0 was overridden with `version` but not `src` at ./pkgs/applications/emulators/wine/nocheckowner.nix:18:3.

This is most likely not what you want. In order to properly change the version of a package, override
both the `version` and `src` attributes:

hello.overrideAttrs (oldAttrs: rec {
  version = "1.0.0";
  src = pkgs.fetchurl {
    url = "mirror://gnu/hello/hello-${version}.tar.gz";
    hash = "...";
  };
})

(To silence this warning, set `__intentionallyOverridingVersion = true` in your `overrideAttrs` call.)

error:
… while querying the derivation named 'tor-browser-13.0.13'

… while calling the 'foldl'' builtin

  at <nixpkgs> + /pkgs/stdenv/generic/check-meta.nix:579:21:

   578|       # if you add a new maintainer or team attribute please ensure that this expectation is still met.
   579|       maintainers = unique (
      |                     ^
   580|         attrs.meta.maintainers or [ ] ++ concatMap (team: team.members or [ ]) attrs.meta.teams or [ ]

… while calling anonymous lambda

  at <nixpkgs> + /lib/lists.nix:1901:25:

  1900|   */
  1901|   unique = foldl' (acc: e: if elem e acc then acc else acc ++ [ e ]) [ ];
      |                         ^
  1902|

error: undefined variable 'joachifm'

at ./pkgs/applications/networking/browsers/tor-browser/default.nix:330:55:

   329|     platforms = attrNames sources;
   330|     maintainers = with maintainers; [ felschr panicgh joachifm hax404 ];
      |                                                       ^
   331|     # MPL2.0+, GPL+, &c.  While it's not entirely clear whether
