trace: evaluation warning: fontforge-20230101 was overridden with `version` but not `src` at ./default.nix:773:5.

This is most likely not what you want. In order to properly change the version of a package, override
both the `version` and `src` attributes:

hello.overrideAttrs (oldAttrs: rec {
  version = "1.0.0";
  src = pkgs.fetchurl {
    url = "mirror://gnu/hello/hello-${version}.tar.gz";
    hash = "...";
  };
})

(To silence this warning, set `__intentionallyOverridingVersion = true` in your `overrideAttrs` call.)

trace: evaluation warning: wine-10.0 was overridden with `version` but not `src` at ./pkgs/applications/emulators/wine/nocheckowner.nix:18:3.

This is most likely not what you want. In order to properly change the version of a package, override
both the `version` and `src` attributes:

hello.overrideAttrs (oldAttrs: rec {
  version = "1.0.0";
  src = pkgs.fetchurl {
    url = "mirror://gnu/hello/hello-${version}.tar.gz";
    hash = "...";
  };
})

(To silence this warning, set `__intentionallyOverridingVersion = true` in your `overrideAttrs` call.)

error:
… while querying the derivation named 'brother-hll6400dw-cupswrapper-3.5.1-1'

error: undefined variable 'milahu'

at ./pkgs/misc/cups/drivers/brother/hll6400dw/hll6400dw.nix:195:39:

   194|     platforms = platforms.all;
   195|     maintainers = with maintainers; [ milahu ];
      |                                       ^
   196|   };
