error:
… while evaluating the attribute 'nginx'
  at ./default.nix:57:3:
    56|     };
    57|   nginx = nginxStable;
      |   ^
    58|   mpvScripts = callPackage ./pkgs/mpv/scripts { };

… while calling a functor (an attribute set with a '__functor' attribute)
  at ./default.nix:45:5:
    44|     in
    45|     nginxStable'.override {
      |     ^
    46|       modules =

… from call site
  at <nixpkgs> + /lib/trivial.nix:1183:7:
  1182|     g: {
  1183|       __functor = self: g;
      |       ^
  1184|       __functionArgs = fArgs;

… while calling anonymous lambda
  at <nixpkgs> + /lib/customisation.nix:171:13:
   170|             */
   171|             newArgs: makeOverridable f (origArgs // (if isFunction newArgs then newArgs origArgs else newArgs))
      |             ^
   172|           );

… while calling a functor (an attribute set with a '__functor' attribute)
  at <nixpkgs> + /lib/customisation.nix:171:22:
   170|             */
   171|             newArgs: makeOverridable f (origArgs // (if isFunction newArgs then newArgs origArgs else newArgs))
      |                      ^
   172|           );

… from call site
  at <nixpkgs> + /lib/trivial.nix:1183:7:
  1182|     g: {
  1183|       __functor = self: g;
      |       ^
  1184|       __functionArgs = fArgs;

… while calling 'f''
  at <nixpkgs> + /lib/customisation.nix:158:9:
   157|       f' =
   158|         origArgs:
      |         ^
   159|         let

… while evaluating a branch condition
  at <nixpkgs> + /lib/customisation.nix:174:9:
   173|         in
   174|         if isAttrs result then
      |         ^
   175|           result

… while calling the 'isAttrs' builtin
  at <nixpkgs> + /lib/customisation.nix:174:12:
   173|         in
   174|         if isAttrs result then
      |            ^
   175|           result

… from call site
  at <nixpkgs> + /lib/customisation.nix:160:20:
   159|         let
   160|           result = f origArgs;
      |                    ^
   161|

… while calling anonymous lambda
  at <nixpkgs> + /pkgs/servers/http/nginx/stable.nix:1:1:
     1| { callPackage, ... }@args:
      | ^
     2|

… while calling a functor (an attribute set with a '__functor' attribute)
  at <nixpkgs> + /pkgs/servers/http/nginx/stable.nix:3:1:
     2|
     3| callPackage ./generic.nix args {
      | ^
     4|   version = "1.30.4";

… from call site
  at <nixpkgs> + /lib/trivial.nix:1083:5:
  1082|     # TODO: Should we add call-time "type" checking like built in?
  1083|     __functor = self: f;
      |     ^
  1084|     __functionArgs = args;

… while calling anonymous lambda
  at <nixpkgs> + /pkgs/servers/http/nginx/generic.nix:32:1:
    31|
    32| {
      | ^
    33|   pname ? "nginx",

… in the condition of the assert statement
  at <nixpkgs> + /pkgs/servers/http/nginx/generic.nix:71:1:
    70|
    71| assert lib.assertMsg (lib.unique moduleNames == moduleNames)
      | ^
    72|   "nginx: duplicate modules: ${lib.concatStringsSep ", " moduleNames}. A common cause for this is that services.nginx.additionalModules adds a module which the nixos module itself already adds.";

… from call site
  at <nixpkgs> + /pkgs/servers/http/nginx/generic.nix:71:8:
    70|
    71| assert lib.assertMsg (lib.unique moduleNames == moduleNames)
      |        ^
    72|   "nginx: duplicate modules: ${lib.concatStringsSep ", " moduleNames}. A common cause for this is that services.nginx.additionalModules adds a module which the nixos module itself already adds.";

… while calling 'assertMsg'
  at <nixpkgs> + /lib/asserts.nix:50:21:
    49|   # TODO(Profpatsch): add tests that check stderr
    50|   assertMsg = pred: msg: pred || throw msg;
      |                     ^
    51|

… in the left operand of the OR (||) operator
  at <nixpkgs> + /lib/asserts.nix:50:31:
    49|   # TODO(Profpatsch): add tests that check stderr
    50|   assertMsg = pred: msg: pred || throw msg;
      |                               ^
    51|

… while calling the 'foldl'' builtin
  at <nixpkgs> + /pkgs/servers/http/nginx/generic.nix:71:23:
    70|
    71| assert lib.assertMsg (lib.unique moduleNames == moduleNames)
      |                       ^
    72|   "nginx: duplicate modules: ${lib.concatStringsSep ", " moduleNames}. A common cause for this is that services.nginx.additionalModules adds a module which the nixos module itself already adds.";

… while calling anonymous lambda
  at <nixpkgs> + /lib/lists.nix:1939:25:
  1938|   */
  1939|   unique = foldl' (acc: e: if elem e acc then acc else acc ++ [ e ]) [ ];
      |                         ^
  1940|

… while evaluating a branch condition
  at <nixpkgs> + /lib/lists.nix:1939:28:
  1938|   */
  1939|   unique = foldl' (acc: e: if elem e acc then acc else acc ++ [ e ]) [ ];
      |                            ^
  1940|

… while calling the 'elem' builtin
  at <nixpkgs> + /lib/lists.nix:1939:31:
  1938|   */
  1939|   unique = foldl' (acc: e: if elem e acc then acc else acc ++ [ e ]) [ ];
      |                               ^
  1940|

… while calling anonymous lambda
  at <nixpkgs> + /pkgs/servers/http/nginx/generic.nix:54:22:
    53|
    54|   moduleNames = map (mod: mod.pname) modules;
      |                      ^
    55|

error: attribute 'pname' missing
at <nixpkgs> + /pkgs/servers/http/nginx/generic.nix:54:27:
    53|
    54|   moduleNames = map (mod: mod.pname) modules;
      |                           ^
    55|
Did you mean name?
